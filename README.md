# Request Bot


## Запуск

1. Установите зависимости: `poetry install`.
2. Настройте `.env` (токен бота, параметры PostgreSQL).
3. Примените миграции: `poetry run alembic upgrade head`.
4. Запустите бота: `poetry run python -m app.main`.

## Основные команды

### Специалист
- "➕ Создать заявку" — пошаговый мастер создания заявки.
- "📄 Мои заявки" — перечень последних запросов.

### Инженер
- `/inspection_schedule <номер> <ДД.ММ.ГГГГ> <ЧЧ:ММ>` — назначить осмотр.
- `/inspection_done <номер> [комментарий]` — завершить осмотр.
- `/add_budget` и `/update_budget` — вести смету.
- `/assign_master <номер> <telegram_id>` — назначить мастера.
- `/ready_for_sign <номер>` — передать на подписание.

### Мастер
- `/start_work <номер>` — зафиксировать старт работ.
- `/finish_work <номер> [часы] [комментарий]` — завершить смену.
- `/update_budget` — внести фактические объёмы и стоимость.
- Фото с подписью `RQ-XXXX комментарий` — добавить фотоотчёт.

### Руководитель
- "📊 Отчёты и статистика" — агрегированный отчёт за месяц.
- `/export_requests` — выгрузка CSV за 30 дней.

### Заказчик
- `/feedback <номер> <качество> <сроки> <культура> [комментарий]` — оставить отзыв.
